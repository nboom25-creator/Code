// Next.js normally supplies this declaration through its own ambient types;
// declaring it here keeps `tsc --noEmit` working before the first `next build`
// has generated .next/types.
declare module "*.css";
