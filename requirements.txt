# Core
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
python-dotenv>=1.0

# Broker / market data (Alpaca)
alpaca-py>=0.20

# Optional: required only for the Robinhood agentic-trading broker
# (config.yaml `broker: robinhood`). Safe to omit if you use Alpaca.
# mcp>=1.0

# Free, keyless market data + news for the dry-run sandbox.
yfinance>=0.2.40

# HTTP client for the alternative-research APIs (FMP, SEC EDGAR, Firecrawl).
requests>=2.31

# Optional: enables the "ml" strategy (machine-learning signal layer).
# Safe to omit — the ML strategy/tests are skipped if it's absent.
scikit-learn>=1.3

# Autonomous LLM agent layer (the `agent` command). Pydantic powers the
# structured cognitive-phase outputs; anthropic is the Claude SDK.
anthropic>=0.40
pydantic>=2.5

# Dev / test
pytest>=7.4
